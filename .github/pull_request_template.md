## Step

`0x__` — <name>

## What this builds

<one or two sentences>

## The problem it solves

<what was broken or missing before this step>

## Proof

- [ ] `go test ./...` passes
- [ ] `go test -race ./...` passes
- [ ] `gofmt -l .` is empty
- [ ] hexdump / benchmark output pasted below, if bytes or performance changed

```
<paste output here>
```

## Known gaps left for later

<what this deliberately does not do yet, and which step handles it>
